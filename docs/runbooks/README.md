# Runbooks

Procedimentos operacionais do the-lab-zone-agents — o "como fazer" recorrente,
distinto dos ADRs (`../decisions/`), que registram o "por que decidimos".

| Runbook | Quando usar |
|---|---|
| [recarregar-config-mcp-toolhive](recarregar-config-mcp-toolhive.md) | Após mudar um ConfigMap consumido por um MCP de memória (ex. `FILTERABLE_FIELDS`) — reiniciar o StatefulSet backend para a mudança entrar. |
